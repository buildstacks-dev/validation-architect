// CF-W01-R — every named refusal class refuses pre-mutation (HB-001).
describe("CF-W01-R", () => {
  it("refuses a duplicate widget id", () => {});
  it("refuses a malformed widget", () => {});
  it("negative control: a seeded duplicate slips a lying store and the detector fires", () => {});
});
