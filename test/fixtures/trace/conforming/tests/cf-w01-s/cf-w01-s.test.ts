// CF-W01-S — widget creation happy path (HB-001; Journey matrix). Synthetic scan subject for
// the trace CLI suite; never executed by vitest (test/fixtures is excluded).
describe("CF-W01-S", () => {
  it("creates a widget", () => {});
  it("persists the widget durably", () => {});
});
